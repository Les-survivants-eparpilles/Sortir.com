<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">

        <meta content="width=device-width, initial-scale=1.0" name="viewport">
        <meta content="" name="keywords">
        <meta content="" name="description">


        <title>{% block title %}Vous n'êtes pas sensés être ici!{% endblock %}</title>
        {% block stylesheets %}

{{ include('inc/_link.html.twig') }}


        {% endblock %}
    </head>
    <body>
    {% block body %}
    <header>
{#include du fichier twig contenant les liens#}
        {{ include('inc/_navbarThemeRegna.html.twig') }}
    </header>

    <main id="main">
        <section id="about">
        {% block main %}
        {% endblock %}
            <div id="msg">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ label }}">
                        {{ message }}
                    </div>
                {% endfor %}
            {% endfor %}
        </div>
    {% endblock %}
        </section>
    </main>

        <footer  >
            <div  id="copyright">&copy;LesSurvivantsEparpillés{{ "now" |date("Y") }}</div>
        </footer>
    <a href="#" class="back-to-top"><i class="fa fa-chevron-up"></i></a>

        {% block javascripts %}
    {{ include('inc/_javascript.html.twig') }}
        {% endblock %}

    </body>
</html>
